function bubbleSort(array) {
	// make swapper
    // make for loop so that it bubble sorts then ignores the last element after sorting
    // get thing that checks if no swaps were made
    // smile cuz it works
	return array;
}

module.exports = bubbleSort;
